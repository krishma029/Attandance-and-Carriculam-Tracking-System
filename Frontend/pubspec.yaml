name: Track_ademy
description: A new Flutter project.

publish_to: 'none' # Remove this line if publishing to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  file_picker: ^8.0.0
  flutter_pdf_text: ^0.9.0
  http: ^1.2.0
  syncfusion_flutter_pdf: ^28.1.33
  docx_to_text: ^1.0.1    # for extracting text from DOCX
  cupertino_icons: ^1.0.8
  percent_indicator: ^4.2.2
  table_calendar: ^3.1.1
  url_launcher: ^6.3.0




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2
  http: ^1.1.0


flutter:
  uses-material-design: true
  assets:
    - assets/


